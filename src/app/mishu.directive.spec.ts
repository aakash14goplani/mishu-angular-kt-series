import { MishuDirective } from './mishu.directive';

describe('MishuDirective', () => {
  it('should create an instance', () => {
    const directive = new MishuDirective();
    expect(directive).toBeTruthy();
  });
});
